"""juggle_vcs_recipes — bundled VCS backend recipe templates for
``/juggle:vcs-backend-init``. Each module is a standalone, loadable VCS plugin
(BACKEND / PROTOCOL_VERSION / optional detect) that ``scaffold`` copies verbatim
into ``~/.juggle/vcs_plugins/<name>.py``.
"""
