# P8 Legacy-Table Drop — Remaining-Work Task Graph (watchdog/autopilot DAG)

> Derived from the DA-hardened plan `plan/2026-06-27-p8-completion.md` (3-round DA, R2 resolved,
> R3-1..R3-4 residuals folded in here). This is NOT a re-plan — it measures what is already landed,
> folds the 4 R3 residuals, and emits the remaining work as an armed DAG. Each node is one atomic
> coder dispatch; every `verify_cmd` exits 0 iff the node's acceptance holds, with NO human.

## Analysis — DONE vs REMAINING (step map + ref count)

```
Step 1 (vocab=open + one engine, Tasks 1.1-1.5)  DONE   68a25ef 331d36a e80bbeb 83cb43e (+migration51, M3 writer)
Step 2 (centralize maps + background, 2.0-2.2)    DONE   372a7e8 333db58 d39e71d (background bijective, gen SQL)
Step 3.0 (delete alias shim, H2)                  DONE   40126af
Step 3.1 (conv READS -> nodes)                    PARTIAL cyc_EC: 37b1ff7 3b425d6 c9b3c89 9517140 (panels/dogfood/cmd_threads/reaper) + ratchet 123->107 (5049d56)
Step 3.1 remainder (cmd_projects/context/startup/summary)  REMAINING -> node c3-reads
Step 3.2 (cut conv WRITE + R3-2 bg tests)         REMAINING -> node c3-write-cut
Step 4.1-4.3 (graph cluster flip, del db_mirror, cut graph_* writes, R3-1 reverse-backfill)  REMAINING -> c4-task-reads c4-topic-dag c4-write-cut
Step 5.1-5.2 (honest DDL + honest Gate-A)         REMAINING -> c5-ddl c5-gate
Step 6.1-6.2-6.4 (dispatch edge M1, discriminator M2, spec as-built + R3-3)  REMAINING -> c6-dispatch-edge c6-discriminator c6-spec
Step 6.3 (TERMINAL DROP, L1, IRREVERSIBLE)        REMAINING -> drop [ESCALATE — never auto-dispatch]
```

**Remaining legacy-ref count:** authoritative gate `scan_legacy_refs(Path('src'))` is pinned at **107**
(ratchet `tests/test_p8_readiness.py::test_static_legacy_ref_floor_ratchet`, `<= 107`, set on cyc_EC@5049d56).
Raw broad grep on main = 284 matches (incl. schema/migration modules the gate excludes). **End-state target: 0.**
The 107 floor is intentionally blocked on Step 4 (the `get_thread`/`get_all_threads` cascade into
`db_mirror`/`orphan_guard`) + the Task 3.2 writer-cut — it only reaches 0 at node `c4-write-cut`.

**R3 residual placement:** R3-1 → `c4-write-cut` (reverse-backfill vocab). R3-2 → `c3-write-cut`
(co-commit background test changes). R3-3 → `c6-spec` (scope "one machine" to executor kinds). R3-4 → every
cluster node re-tightens the ratchet; `c4-write-cut` asserts it == 0.

**Plan/DA contradiction hit (see --retain):** the plan's Task 3.1 lists `juggle_cmd_selfheal.py` as a
conversation consumer, but its `row['status']` is the **selfheal error-event** lifecycle (`'diagnosing'`),
NOT `threads.status` — a false positive. `c3-reads` EXCLUDES it from the grep-clean (including it would
red the gate). Likewise `c4-write-cut`'s R3-1 fix: graph_tasks vocab was unified to node-state (`open`/`done`)
in Step 1, so reverse-backfill must write the node-state value **directly (identity)**, NOT via
`status_for_state` (which emits the thread vocab `active`/`closed`) as the plan's Task 4.3 prose says.

**Per-node convention:** every dispatch prompt ends with — "Before calling `complete-agent`, invoke
`mike:pre-pr` skill to run the quality gate. Do NOT open a PR." Each `verify_cmd` assumes the three import-time
env vars are set inline. `make test` (full suite) green is the per-commit invariant from the plan; the
targeted pytest in each `verify_cmd` is the node-specific proof.

### Execution Status (2026-06-29) — main @ 17afe49
- **All 10 graph nodes VERIFIED + MERGED to main:** c3-reads (a0ad6d4 +5391f5f,879082b,febdb6e),
  c3-write-cut (c1e785c +e481c90), c4-task-reads (cb09553), c4-topic-dag (43c0da9 +ed8a184,17afe49),
  c4-write-cut (ed44cc2 +bb8f37c,a69483c,e3e534e), c5-ddl (94d2d1d), c5-gate (270e63e),
  c6-dispatch-edge (a890e55 +99428ed), c6-discriminator (a3a9d5c +ff2b7ae), c6-spec (b0ebbb1).
- **Reads-collapse DONE:** get_thread/get_all_threads conversation reads resolve from kind='conversation'
  nodes (ed8a184, 17afe49). `scan_legacy_refs(src)` now = **47** (was 125). Overnight: F1 (9c695a1), F2 (bcfd9f5).
- **REMAINING = writes-collapse + migrate_lifecycle delete + DROP.** Writes-collapse (cut legacy
  graph_tasks/graph_topics writes in db_graph.py/db_topics.py + remaining threads writes -> nodes/node_edges)
  drives static.fail 47 -> ~9; c4-write-cut's `static.fail==0` acceptance is NOT yet met. Then delete
  juggle_migrate_lifecycle.py (file present, 0 src importers), then the terminal IRREVERSIBLE `drop` (backup
  present at ~/.claude/juggle/juggle.db.bak-pre-p8).

**Execution Status (2026-06-29) — COMPLETE:** Migration 55 dropped all 4 legacy tables on prod (`0af54e5`, v1.86.0); Migration 56 repointed notifications FK (`a694c04`/`38fd31f`, v1.86.1); static.fail=0; suite 2719/0; nodes sole store.

---

## c3-reads: Flip remaining conversation READ consumers to nodes (Task 3.1 remainder)
verify_cmd: export CLAUDE_PLUGIN_DATA="$HOME/.claude/juggle" JUGGLE_MAX_BACKGROUND_AGENTS=5 JUGGLE_MAX_THREADS=10 && ! git grep -qnE "FROM threads|\['status'\]|\['topic'\]|\['last_active'\]" -- src/juggle_cmd_projects.py src/juggle_cmd_context.py src/juggle_context_startup.py src/juggle_project_summary.py src/juggle_cmd_runs.py src/juggle_cmd_agents_lifecycle.py && uv run pytest tests/test_cmd_context.py tests/test_cockpit_model.py tests/test_p8_conv_read_collapse.py -q

Implement the remainder of Task 3.1 in `plan/2026-06-27-p8-completion.md` §STEP 3. Flip these conversation
display consumers from `threads` to `nodes WHERE kind='conversation'`, adopting `state`/`title`/`last_active_at`
and value-mapping every status compare through `node_translation.STATUS_TO_STATE` (NEVER re-type the map inline):
- `src/juggle_cmd_projects.py` (`SELECT … FROM threads` at :223,:299,:337; `row['status']` :537; `row['topic']` :503,:537)
- `src/juggle_cmd_context.py` (`status == "active"` :141, `status == "background"` :145)
- `src/juggle_context_startup.py` (`status == "background"` :32,:200)
- `src/juggle_project_summary.py` (`thread['topic']` :51,:58)
- `src/juggle_cmd_runs.py`, `src/juggle_cmd_agents_lifecycle.py` (any residual `row['status']`/`['topic']` conversation reads — grep-clean must hold)
EXCLUDE `src/juggle_cmd_selfheal.py` — its `row['status']` is the selfheal error-event state (`'diagnosing'`),
a different table/vocab; do NOT touch it (the plan's listing of it is a false positive).
This is NOT a mechanical rename — each `['status']`→`['state']` carries a VALUE translation
(`active→open`, `background→background` bijective, `running→running`, `closed→done`, `archived→archived`).
TDD pin (per consumer): a behavior test over a `nodes`-seeded DB asserting the consumer lists/filters the
conversation by its node state (extend `tests/test_cmd_context.py` / `tests/test_cockpit_model.py`).
After flipping, lower the ratchet pin in `tests/test_p8_readiness.py::test_static_legacy_ref_floor_ratchet`
to the new (smaller) count (R3-4). Commit per consumer. Before calling `complete-agent`, invoke `mike:pre-pr`
skill to run the quality gate. Do NOT open a PR.

---

## c3-write-cut: Cut the legacy conversation WRITE; co-commit background tests (Task 3.2 + R3-2)
deps: c3-reads
verify_cmd: export CLAUDE_PLUGIN_DATA="$HOME/.claude/juggle" JUGGLE_MAX_BACKGROUND_AGENTS=5 JUGGLE_MAX_THREADS=10 && ! git grep -qnE "update_thread\([^)]*status=.background|UPDATE threads SET status" -- src/juggle_dispatch_core.py src/juggle_watchdog.py && PYTHONPATH=src uv run python -c "import dbops.threads, juggle_watchdog, juggle_dispatch_core, juggle_cmd_context, juggle_context_startup" && uv run pytest tests/test_dispatch_node.py tests/test_db_thread_state.py tests/test_p8_conv_read_collapse.py tests/test_thread_label_alloc_atomic.py -q

Implement Task 3.2 in `plan/2026-06-27-p8-completion.md` §STEP 3. Make `nodes` the SOLE conversation writer:
- Flip the three background WRITERS to write `nodes.state='background'` via the Task-1.5 `write_state` helper:
  `src/juggle_dispatch_core.py:103`, `src/juggle_watchdog.py:761,:955` (drop their `update_thread(status="background")`).
- Flip the slug/live-thread write-path reads to `nodes`: `src/dbops/slug_alloc.py:37,:116` (`LIVE_SLUG_STATES`
  → node-state set `("open","running","background")`) and `src/dbops/threads.py:88,:95,:150,:322,:340,:377`;
  reproduce the partial UNIQUE INDEX `idx_threads_live_label` on `nodes(user_label) WHERE kind='conversation'
  AND state IN ('open','running','background')` in a new conversation-cluster migration (the slug-collision guard —
  `background` MUST stay in the predicate, the load-bearing proof of R2-1's first-class state).
- Remove the conversation divergence-hiders: `dbops/threads.py:108-109`, `dbops/conv_node_mirror.py:32`
  (narrow to "no such table: nodes" only if Step 5 DDL not yet landed), `dbops/messages.py:77-78`.
- **R3-2 (green-per-commit):** `tests/test_dispatch_node.py` and `tests/test_db_thread_state.py` currently
  assert `background` via the legacy `threads.status`. Rewrite those assertions to read `nodes.state='background'`
  and co-commit them IN THE SAME COMMIT as the writer-cut so the suite never reds mid-sequence (rewrite through
  the new seam — do NOT delete the pins). Rewrite `tests/test_thread_label_alloc_atomic.py` over `nodes` (same
  invariant: a live `background` slug is never recycled).
TDD pin: a conversation create writes exactly one authoritative row (`nodes`); a missing `nodes` COLUMN raises
(not swallowed). After this node, `scan_legacy_refs` is strictly < the Step-2 value but NOT yet 0 (the
`get_thread`/`get_all_threads` cascade is reserved for `c4-topic-dag`). Lower the ratchet pin to the new count (R3-4).
Before calling `complete-agent`, invoke `mike:pre-pr` skill to run the quality gate. Do NOT open a PR.

---

## c4-task-reads: Flip task-execution reads to nodes/node_edges (Task 4.1)
deps: c3-write-cut
verify_cmd: export CLAUDE_PLUGIN_DATA="$HOME/.claude/juggle" JUGGLE_MAX_BACKGROUND_AGENTS=5 JUGGLE_MAX_THREADS=10 && uv run pytest tests/test_db_graph.py tests/test_graph_dispatch.py tests/test_graph_scheduler.py tests/test_node_transition.py -q

Implement Task 4.1 in `plan/2026-06-27-p8-completion.md` §STEP 4. Repoint the task-execution READERS to
`nodes`/`node_edges` (`kind='task'`; deps from `node_edges`):
- `src/dbops/db_graph.py` — `get_task`, `get_task_by_thread`, `list_tasks`, `ready_eligible`, `unverified_deps`,
  `get_deps`/`get_dependents`/`replace_edges`.
- `src/juggle_graph_dispatch.py` — `claim_task`/`sweep_stale_claims` CAS on `nodes`.
TDD pin: assert `ready_eligible`/`claim_task` operate when ONLY `nodes`/`node_edges` carry the task (seed NO
`graph_tasks` row) — add to `tests/test_db_graph.py` / `tests/test_graph_dispatch.py`. Dual-write to `graph_*`
stays ON (cut in `c4-write-cut`) so reads of either store agree. Before calling `complete-agent`, invoke
`mike:pre-pr` skill to run the quality gate. Do NOT open a PR.

---

## c4-topic-dag: Flip topic/DAG/orphan reads; DELETE db_mirror (Task 4.2 + R2-5)
deps: c4-task-reads
verify_cmd: export CLAUDE_PLUGIN_DATA="$HOME/.claude/juggle" JUGGLE_MAX_BACKGROUND_AGENTS=5 JUGGLE_MAX_THREADS=10 && ! git grep -ql db_mirror -- src/ && [ ! -f src/dbops/db_mirror.py ] && PYTHONPATH=src uv run python -c "import dbops.threads, dbops.db_topics, dbops.db_topics_reconcile, juggle_cmd_threads, juggle_cmd_projects, juggle_cmd_doctor" && uv run pytest tests/test_cockpit_graph_dag_load.py tests/test_graph_reconcile.py -q

Implement Task 4.2 in `plan/2026-06-27-p8-completion.md` §STEP 4. Flip the topic-tier + DAG + orphan readers
to `nodes`, and DELETE the dead mirror concept:
- `src/dbops/db_topics.py` (CRUD/queries → `nodes WHERE kind='task' AND parent_id IS NULL`),
  `src/dbops/db_topics_reconcile.py`, `src/juggle_cockpit_graph_dag.py` (drop the legacy fallback; nodes only),
  `src/dbops/orphan_guard.py:48-66,121-132,164-184,214-224` (resolve thread/dispatch binding via `nodes`).
- Flip the shared `dbops/threads.py` `get_thread`/`get_all_threads` full-row readers to `nodes` (this is the
  cascade the 107 floor was blocked on).
- DELETE `src/dbops/db_mirror.py` AND every importer — enumerate with `git grep -l db_mirror -- src/` (do NOT
  hand-list); R2-5: importers include `dbops/threads.py`, `dbops/db_topics.py`, `dbops/db_topics_reconcile.py`,
  `juggle_cmd_doctor.py`, `juggle_cmd_projects.py`, **`juggle_cmd_threads.py`**. Removing the module without
  flipping `threads.py`+`cmd_threads.py` raises ImportError at this commit (green-per-commit break).
- DELETE `tests/test_db_mirror.py` + `tests/test_graph_mirror.py` (obsolete concept) — but FIRST confirm no pin
  inside guards a still-live invariant ("mirror topics never dispatched"); if so rewrite it over `nodes`
  ("conversation nodes never enter the task dispatch set").
TDD pin: cockpit DAG renders topic+task nodes purely from `nodes` with no `graph_*` table present
(`tests/test_cockpit_graph_dag_load.py`). Lower the ratchet pin (R3-4). Run `cockpit --smoke --all-viewports`
and paste the summary. Before calling `complete-agent`, invoke `mike:pre-pr` skill to run the quality gate.
Do NOT open a PR.

---

## c4-write-cut: Cut legacy graph_* writes; reverse-backfill with corrected vocab; static.fail→0 (Task 4.3 + R3-1 + R3-4)
deps: c4-topic-dag
verify_cmd: export CLAUDE_PLUGIN_DATA="$HOME/.claude/juggle" JUGGLE_MAX_BACKGROUND_AGENTS=5 JUGGLE_MAX_THREADS=10 && ! git grep -qnE "INSERT INTO graph_tasks|INSERT INTO graph_edges|INSERT OR IGNORE INTO graph_" -- src/juggle_add_node.py && PYTHONPATH=src uv run python -c "from pathlib import Path; from dbops.p8_readiness import scan_legacy_refs; n=len(scan_legacy_refs(Path('src'))); assert n==0, f'static.fail={n}'; print('static.fail=0')" && uv run pytest tests/test_p8_reverse_backfill.py tests/test_add_node.py tests/test_dispatch_node.py tests/test_p8_readiness.py -q

Implement Task 4.3 in `plan/2026-06-27-p8-completion.md` §STEP 4 (completes C1).
- `src/juggle_add_node.py:216-246` — DELETE the `graph_tasks` + `graph_edges` INSERTs (keep `nodes`/`node_edges`);
  drop the dual-write contract from the docstring.
- `src/dbops/state_write.py` — drop the `graph_tasks`/`graph_topics` mirror half (`nodes` only).
- Remove any remaining `except sqlite3.OperationalError: pass` hiding a graph write (`conv_node_mirror`,
  `messages`, `threads`) now that `nodes` is guaranteed present.
- Create `src/dbops/p8_reverse_backfill.py::reverse_backfill_nodes_to_graph(conn)` — idempotent inverse for the
  R2-3 rollback. **R3-1 (CORRECTED VOCAB):** graph_tasks state was unified to the node vocab (`open`/`ready`/
  `done`/…) in Step 1, so re-INSERT `graph_tasks.state` with the node-state value **directly (identity)** — do
  NOT route through `node_translation.status_for_state` (that emits the thread vocab `active`/`closed` and would
  write a state the restored legacy engine cannot transition). Re-create `graph_edges` from `node_edges WHERE
  kind='dep'`.
- **Step 0 backup (runbook):** record that `cp ~/.claude/juggle/juggle.db ~/.claude/juggle/juggle.db.bak-pre-p8-step4`
  must run before this commit reaches production (this commit makes `nodes` the sole store — the irreversibility
  boundary is HERE, not Step 6).
TDD pins: (a) C1 capstone — a task driven `open→…→done` via `node_transition` writes ZERO `graph_tasks` rows
(`tests/test_add_node.py`); (b) **R3-1** — `tests/test_p8_reverse_backfill.py` asserts the reconstructed
`graph_tasks.state` equals the node-state value (e.g. `'open'`/`'done'`), NOT `'active'`/`'closed'`, and that a
second run no-ops. Tighten `tests/test_p8_readiness.py::test_static_legacy_ref_floor_ratchet` to `<= 0` (R3-4).
Before calling `complete-agent`, invoke `mike:pre-pr` skill to run the quality gate. Do NOT open a PR.

---

## c5-ddl: Fold the 4 parity columns into CREATE_NODES (Task 5.1, H4)
deps: c4-write-cut
verify_cmd: export CLAUDE_PLUGIN_DATA="$HOME/.claude/juggle" JUGGLE_MAX_BACKGROUND_AGENTS=5 JUGGLE_MAX_THREADS=10 && uv run pytest tests/test_nodes_schema_migration.py -q && PYTHONPATH=src uv run python -c "import sqlite3; from dbops.schema_nodes import CREATE_NODES; c=sqlite3.connect(':memory:'); c.execute(CREATE_NODES); cols={r[1] for r in c.execute('PRAGMA table_info(nodes)')}; need={'user_label','assigned_by','last_active_at','dispatch_thread_id'}; assert need<=cols, need-cols; print('DDL complete')"

Implement Task 5.1 in `plan/2026-06-27-p8-completion.md` §STEP 5.
- `src/dbops/schema_nodes.py:8-57` — add `user_label TEXT`, `assigned_by TEXT NOT NULL DEFAULT 'auto'`,
  `last_active_at TEXT`, `dispatch_thread_id TEXT` to `CREATE_NODES` (keep the additive ALTERs in
  `migration_nodes_parity` as idempotent no-ops for upgrades).
- `src/dbops/conv_node_mirror.py:32` — now safely REMOVE the blanket `except OperationalError: pass`
  (fresh DDL is complete; a real gap must fail loud).
TDD pin: `test_create_nodes_is_complete` — a fresh `nodes` from `CREATE_NODES` alone (no migrations) contains
every column the mirror writes. Run `doctor --dry-run` smoke on a fresh tmp DB. Before calling `complete-agent`,
invoke `mike:pre-pr` skill to run the quality gate. Do NOT open a PR.

---

## c5-gate: Honest Gate-A — scan de-excluded engines, log exclusions+imports (Task 5.2, M4)
deps: c5-ddl
verify_cmd: export CLAUDE_PLUGIN_DATA="$HOME/.claude/juggle" JUGGLE_MAX_BACKGROUND_AGENTS=5 JUGGLE_MAX_THREADS=10 && uv run pytest tests/test_p8_readiness.py -q && PYTHONPATH=src uv run python -c "import sqlite3; from pathlib import Path; from dbops.p8_readiness import pre_p8_report; r=pre_p8_report(sqlite3.connect(':memory:'), Path('src')); assert isinstance(r['static']['excluded_files'], list); assert r['static']['import_refs']==0, r['static']['import_refs']; print('gate honest')"

Implement Task 5.2 in `plan/2026-06-27-p8-completion.md` §STEP 5.
- `src/dbops/p8_readiness.py` — `_excluded` no longer skips `db_graph`/`db_topics`/`db_mirror` (they are no
  longer schema/migration files); add `import_refs(src_root)` asserting no module imports a still-legacy engine
  and that `db_mirror` is gone; `pre_p8_report` emits `excluded_files` (list) + `import_refs` (== 0).
- `src/juggle_cmd_doctor_p8.py` — surface the new fields.
TDD pin: `test_gate_a_reports_exclusions_and_imports` — `excluded_files` is a list, `import_refs == 0`. Before
calling `complete-agent`, invoke `mike:pre-pr` skill to run the quality gate. Do NOT open a PR.

---

## c6-dispatch-edge: Model the dispatch relation as a typed node_edge (Task 6.1, M1/Q2)
deps: c5-gate
verify_cmd: export CLAUDE_PLUGIN_DATA="$HOME/.claude/juggle" JUGGLE_MAX_BACKGROUND_AGENTS=5 JUGGLE_MAX_THREADS=10 && uv run pytest tests/test_dispatch_node.py tests/test_nodes_schema_migration.py -q && [ "$(git grep -lnE 'dispatch_thread_id' -- src/ | grep -vE 'migration_|schema_nodes' | wc -l | tr -d ' ')" = "0" ]

Implement Task 6.1 in `plan/2026-06-27-p8-completion.md` §STEP 6 (recommended OQ2 shape: typed `node_edges` edge).
- Create `src/dbops/migration_52_dispatch_edge.py` — add `node_edges.kind TEXT NOT NULL DEFAULT 'dep'`; migrate
  `nodes.dispatch_thread_id` → a `kind='dispatch'` edge `(task_node_id, conversation_node_id)`; the raw column is
  dropped in the terminal rebuild (`drop`).
- Update every `node_edges` query to filter `kind='dep'` for dependency logic and `kind='dispatch'` for the
  agent-thread binding (`orphan_guard`, dispatch hydration). Document the edge kinds in `schema_nodes.py`.
TDD pin: the task→dispatch-thread link round-trips through the typed edge (`tests/test_dispatch_node.py`); after
this node `dispatch_thread_id` appears in `src/` ONLY inside the `migration_5x` that retires it. Before calling
`complete-agent`, invoke `mike:pre-pr` skill to run the quality gate. Do NOT open a PR.

---

## c6-discriminator: Enforce the kind discriminator on nodes (Task 6.2, M2)
deps: c6-dispatch-edge
verify_cmd: export CLAUDE_PLUGIN_DATA="$HOME/.claude/juggle" JUGGLE_MAX_BACKGROUND_AGENTS=5 JUGGLE_MAX_THREADS=10 && uv run pytest tests/test_nodes_schema_migration.py -q

Implement Task 6.2 in `plan/2026-06-27-p8-completion.md` §STEP 6.
- `src/dbops/schema_nodes.py` — add `CHECK` constraints (e.g. `CHECK (kind='task' OR verify_cmd IS NULL)`;
  conversation-only columns NULL for non-conversation), applied on the rebuild in the terminal-drop migration
  (SQLite can't add CHECK via ALTER), OR a single insert/update guard in the node-write path if the rebuild is
  deferred. Keep ONE wide `nodes` table (do NOT split per-kind).
TDD pin: each illegal cross-kind insert is rejected; `SELECT COUNT(*) FROM nodes WHERE kind='conversation' AND
verify_cmd IS NOT NULL` pinned at 0. Before calling `complete-agent`, invoke `mike:pre-pr` skill to run the
quality gate. Do NOT open a PR.

---

## c6-spec: Demote spec to as-built; scope the "one machine" claim (Task 6.4, H5 + R3-3)
deps: c5-gate, c6-dispatch-edge
verify_cmd: export CLAUDE_PLUGIN_DATA="$HOME/.claude/juggle" JUGGLE_MAX_BACKGROUND_AGENTS=5 JUGGLE_MAX_THREADS=10 && uv run pytest tests/test_spec_as_built.py -q && ! git grep -qnE "LOCKED design|^LOCKED" -- specs/2026-06-18-unified-topic-graph.md

Implement Task 6.4 in `plan/2026-06-27-p8-completion.md` §STEP 6, plus residual R3-3.
- `specs/2026-06-18-unified-topic-graph.md` — header `LOCKED design` → `SUPERSEDED — see as-built addendum`;
  add an addendum recording resolved Q1–Q4, the single-model/single-machine end-state, the typed dispatch edge,
  and that §10's "eliminated" list is now actually true.
- **R3-3 (scope the claim):** scope "ONE state machine" to the **executor (task) kinds**. The conversation/
  background machine entries added in Task 2.0 (`('open','dispatch_bg')`, `('background', …)`) are currently
  UNEXERCISED scaffolding — the conversation writers (`dispatch_core`, `watchdog`) set `state='background'`
  directly via `write_state`, no caller routes through `node_transition(...,'conversation')`. State this honestly
  in the addendum (one machine governs task lifecycle; conversation entries are reserved scaffolding, zero callers).
TDD pin: a new `tests/test_spec_as_built.py` asserting (a) `'pending'` is absent from live `src/` (excluding the
dated migrations `migrations_nodes.py`/`migration_51_state_vocab.py`), (b) `dispatch_thread_id` is absent from
STEADY-STATE query code (exclude `migration_*`/`schema_nodes.py` — the raw column legitimately persists in the
schema/migrations until the terminal `drop` rebuild; this node only proves no live query still reads it, true once
`c6-dispatch-edge`'s typed edge lands), (c) the spec carries no bare `LOCKED design` without an as-built note, and
(d) each §10 "eliminated" item is absent from `src/`. Do NOT assert the raw column is gone from the schema here —
that only becomes true after `drop`. Before calling `complete-agent`, invoke `mike:pre-pr` skill to run the quality
gate. Do NOT open a PR.

---

## drop: DROP legacy tables [ESCALATE — IRREVERSIBLE, requires explicit user approval + verified DB backup]
deps: c4-write-cut, c5-ddl, c5-gate, c6-dispatch-edge, c6-discriminator, c6-spec
verify_cmd: export CLAUDE_PLUGIN_DATA="$HOME/.claude/juggle" JUGGLE_MAX_BACKGROUND_AGENTS=5 JUGGLE_MAX_THREADS=10 && [ -f "$HOME/.claude/juggle/juggle.db.bak-pre-p8" ] && [ ! -f src/juggle_migrate_lifecycle.py ] && uv run pytest tests/test_migration_53_p8_drop.py tests/test_doctor.py tests/test_p8_readiness.py -q

**⚠️ DO NOT AUTO-DISPATCH.** This is the terminal, IRREVERSIBLE legacy-table DROP (Task 6.3 of
`plan/2026-06-27-p8-completion.md`). The watchdog/autopilot MUST HALT before this node — it requires explicit
user approval AND a verified, restorable DB backup (`~/.claude/juggle/juggle.db.bak-pre-p8`) taken immediately
before the drop. All upstream nodes must have verified green (`scan_legacy_refs==0`, `import_refs==0`, honest
DDL, dispatch edge + discriminator in place, spec as-built). The `verify_cmd` deliberately asserts the backup
artifact exists and the migration/doctor pins pass — it does NOT itself perform or authorize the drop.

When (and only when) the user authorizes:
- Create `src/dbops/migration_53_p8_drop.py` — `BEGIN IMMEDIATE` fail-loud, gated at top by
  `p8_readiness.p8_drop_ready`; FK-repoint `messages`/`notifications`/`notifications_v2`/`action_items`
  `thread_id REFERENCES nodes(id)` via the table-rebuild idiom; DROP `graph_edges`,`graph_tasks`,`graph_topics`,
  `threads` in FK order; rebuild `nodes` with the c6-discriminator CHECK constraints + drop `dispatch_thread_id`.
  Take the `juggle.db.bak-pre-p8` backup first (doctor research §4.1). Idempotent: re-run → `already-dropped`.
- `src/juggle_db.py:141,155-157` — DELETE the base `CREATE_THREADS`/`CREATE_GRAPH_*` lines so `init_db` does not
  re-create the dropped tables. Remove dead `CREATE_GRAPH_*` (`schema_graph.py`) + `CREATE_THREADS`+FK clauses
  (`schema.py`).
- DELETE `src/juggle_migrate_lifecycle.py` and its 9 refs (L1/Q4). Wire migration 53 after 52 in
  `migrations_recent.py`.
TDD pins: (a) `p8_drop_ready`-green tmp DB → migration drops all 4 tables, `init_db` does NOT re-create them,
messages repointed; (b) `p8_drop_ready`-blocked DB → no drop, reasons surfaced; (c) `test -f
src/juggle_migrate_lifecycle.py` false. Before calling `complete-agent`, invoke `mike:pre-pr` skill to run the
quality gate. Do NOT open a PR.
