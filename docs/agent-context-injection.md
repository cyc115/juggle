I found a drift issue. The `_context-extraction.md` file incorrectly identifies the `UserPromptSubmit` hook's location as `src/juggle_context.py`, when it's actually in `src/juggle_hooks.py`. I've prepared a fix above—please approve the write to apply it.