# Juggle Weekly Digest — 2026-05-25

> Generated by `/schedule:reflect` via Claude Code Routines.
> This digest covers 2026-05-18 through 2026-05-25.
> Autofix PR this week: not run this week
> Dogfood report (Sat): `reports/dogfood-*.md` — **not found** (first run in this environment)
> Autofix PR link (Sun): `reports/autofix-PR-*` — **not found** (first run in this environment)

---

## RF-1 Watchdog Health

*DB query failed — unable to retrieve telemetry.*

> **Note:** No live `juggle.db` present in this ephemeral environment. This is the first
> `schedule-reflect` run; once the DB is populated via normal Juggle usage the watchdog
> event counts will appear here automatically.

## RF-2 Action Item Fatigue

*DB query failed.*

> No `action_items` table rows found — DB not yet initialised in this environment.

## RF-3 Agent Output Quality

*DB query failed.*

> No `agent_completions` rows — DB not yet initialised in this environment.

## RF-4 Context Bloat Candidates

*Messages table not available.*

> No `messages` rows — DB not yet initialised in this environment.

## RF-5 Memory Health

*Hindsight unavailable this week.*

> `HindsightClient.from_config()` returned `None` — Hindsight service not configured
> in this environment.

## RF-6 Auto-Memory Contradictions

*No auto-memory directories found.*

> `~/.claude/projects/*/memory/` glob matched nothing. Auto-memory will populate
> as Claude Code sessions accumulate.

## RF-7 Skill Drift

*No agent task prompts in DB to compare against.*

> DB empty — no historical agent tasks to diff against skill descriptions.

## RF-8 Dogfood Pulse

*No dogfood reports found this week.*

> `reports/dogfood-*.md` does not exist yet. Saturday's `schedule-dogfood` routine
> has not run in this environment. Once it does, this section will surface the
> latest suggested improvements automatically.

---

### Cross-links

| Run | Report | Status |
|-----|--------|--------|
| Saturday dogfood | `reports/dogfood-YYYY-MM-DD.md` | ❌ Not found — first run |
| Sunday autofix PR | `reports/autofix-PR-YYYY-MM-DD` | ❌ Not found — first run |

### Run metadata

| Field | Value |
|-------|-------|
| Routine | `schedule-reflect` |
| Run date | 2026-05-25 (Monday) |
| Cost | $0.0000 (no LLM calls — all sections used static fallbacks) |
| Issues filed | 0 |
| Sections present | 8 / 8 |
| Cost cap | $2.00 per-section (v1.23.1 enforcement) |
| DB path | No live DB in this ephemeral environment |
