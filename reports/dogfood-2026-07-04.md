# Juggle Self-Analysis — 2026-07-04

> Generated by `/schedule:dogfood` via Claude Code Routines.
> Data window: 2026-06-27 to 2026-07-04 (7 days)
> Agent: researcher, model: claude-sonnet-4-6
> LLM cost this run: $0.2053

The database was initialized today (2026-07-04T08:05:07 UTC) — it's a fresh install with zero rows in all operational tables. Here is the structured report:

---

## Observed Friction Patterns

**No data available.** The juggle.db at `/root/.claude/juggle/juggle.db` was initialized at `2026-07-04T08:05:07 UTC` — the same day as this analysis. All operational tables (`nodes`, `agent_runs`, `watchdog_events`, `action_items`, `agent_completions`, `error_events`, `selfheal_audit`) contain **0 rows**. There are fewer than 5 threads/nodes in the window; findings are not representative and no friction patterns can be inferred from this dataset.

---

## Repeated Dispatches / Blockers

None observable. Zero `agent_runs` rows means no dispatch history exists in this DB instance. The `nodes` table (which replaced the legacy `threads` table via migrations through M67) contains 0 rows.

---

## Unresolved Open Questions

None observable. The `action_items` table (which tracks open questions surfaced by agents) has 0 rows. The `error_events` table has 0 rows.

---

## Suggested Improvements (1–3)

Since there is no thread history to base improvements on, the following are structural observations from schema inspection that represent latent friction risks once the system is in active use:

**1. `error_events` has no `created_at` column — date-range queries fail silently**
`error_events` uses `first_seen`/`last_seen` but no `created_at`. Any query scoped by `created_at` (as the prompt specified) will raise `OperationalError` instead of returning empty results. The analysis script had to catch this as an exception.
File ref: `src/dbops/schema_selfheal.py` (see `CREATE TABLE error_events` DDL) — add a `created_at TEXT NOT NULL DEFAULT (strftime('%Y-%m-%dT%H:%M:%fZ','now'))` column in the next migration, or alias `first_seen` as `created_at` in query helpers.

**2. `selfheal_audit` has no `status` column — self-heal outcome is unqueryable**
`selfheal_audit` stores `action`/`reason`/`detail` but not a terminal `status`. Determining whether a self-heal resolved or failed requires joining against `error_events.status`, which is a two-step lookup that isn't surfaced in any CLI report. Add a `resolved INTEGER NOT NULL DEFAULT 0` column to `selfheal_audit` (migration via `dbops/migration_seq.py`) so weekly audits can directly count `WHERE resolved=0`.
File ref: `src/dbops/schema_selfheal.py` — `selfheal_audit` DDL.

**3. `agent_runs` uses `dispatched_at`/`completed_at` but the metrics CLI queries `created_at`**
The `agent_runs` schema uses `dispatched_at` as the creation timestamp, but any cross-table date-scoped query that assumes a consistent `created_at` column across tables will silently return 0 rows. The analysis query (`WHERE created_at >= ?`) failed with 0 results because the column is named differently.
File ref: `src/dbops/schema_runs.py` — consider aliasing or adding `created_at` as a generated column: `created_at TEXT GENERATED ALWAYS AS (dispatched_at) STORED` for query consistency, or normalize the column name across all tables.

---

## Raw Thread Summary (for archival)

```
DB path:          /root/.claude/juggle/juggle.db
DB size:          290,816 bytes (schema-only, migrated to M67)
DB initialized:   2026-07-04T08:05:07 UTC  (today — fresh install)
Analysis window:  2026-06-27 → 2026-07-04

Table            Rows
───────────────────────────────────
nodes              0   (P8 unified table, replaces threads)
agent_runs         0
watchdog_events    0
action_items       0
agent_completions  0
error_events       0
selfheal_audit     0
projects           1   (INBOX sentinel only)
settings           1   (thread_auto_archive_ttl_secs=3600)
migration_seq      1   (next_number=67, schema current)

Verdict: Insufficient data. 0 threads/nodes in the target window.
         Report reflects schema-level findings only.
         Re-run after ≥5 topics/tasks have completed dispatch cycles.
```
